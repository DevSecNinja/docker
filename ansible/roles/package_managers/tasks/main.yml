---
# Tasks for Package Managers role
- name: Install Homebrew
  ansible.builtin.include_tasks: install_homebrew.yml
  tags: [package_managers, homebrew]

- name: Install mise
  ansible.builtin.include_tasks: install_mise.yml
  when: install_mise | bool
  tags: [package_managers, mise]
