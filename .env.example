# Environment variables for the Docker Infrastructure repository
# Loaded by mise automatically

# Ansible configuration
ANSIBLE_CONFIG="${PWD}/ansible.cfg"

# Enable mise verbose output (optional, uncomment for debugging)
# MISE_DEBUG=1
