# Dependencies for the Docker Infrastructure repository
# Install with: pip install -r requirements.txt

# Ansible and related tools
ansible==14.3.1
ansible-lint==26.8.0
# Jinja2 is installed as an Ansible dependency; no direct pin needed.

# YAML linting
yamllint==1.38.0

# Testing frameworks
molecule==26.8.0
molecule-plugins[docker]==26.7.15
